#pragma once /* Защита от двойного включения */

/* Подключение переносимых типов данных */
#include <cstdint>

/* 
Данные компонентов — только простые данные; логика — в системах и World.
Позиция на дискретной сетке ; коммит 2 добавит связь с Grid. 
*/
struct Position
{
    std::int32_t x = 0;
    std::int32_t y = 0;
};

/*
Направление движения
dx - изменение координаты по оси x
dy - изменение координаты по оси y
*/
struct Velocity
{
    std::int8_t dx = 0;
    std::int8_t dy = 0;
};